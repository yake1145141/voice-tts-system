# 每行一条文本，空行与 # 开头的注释行会被忽略
# 用法：python tools/tts_client.py batch tools/sample_texts.txt --concurrency 2
你好，这是第一条语音测试。
今天天气不错，适合出去走走。
括号里的内容不会被朗读（开心地笑）[挥手]。
英文句子也可以合成：Hello, this is a TTS test.
这一条比较长，用来观察 CPU 或 GPU 在不同文本长度下的推理耗时变化，比如排队、超时和降级逻辑是否按预期工作。
缓存复用测试：这一行连续发送两次，第二次会直接命中服务端缓存。
